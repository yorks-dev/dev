#include <stdio.h>
#include <stdlib.h>

/* calloc declerations
 void * realloc(void *ptr, size_t size); */

/* ptr can be NULL but it can also have memory allocated to it
 * if it is not NULL -> then it expands the memory.
 * IT ALWAYS GIVES US CONTEGIOUS MEMORY.
 * It does NOT INITIALIZE the data.
 */

/* FREE    free()  -> void free(void *ptr);
 * frees memory located ion that position.
 * ptr = NULL;    (ALWAYS DO THIS)
*/
