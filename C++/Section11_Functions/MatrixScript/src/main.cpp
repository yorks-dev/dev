#include <iostream>
#include <string>
#include <vector>
using namespace std;

string MatrixScript (vector<vector<string>> script ){

    int N {stoi(script.at(0).at(0))};
    int M {stoi(script.at(0).at())}

}